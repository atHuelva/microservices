server:  
  port: 8888 #puerto donde vamos a levantar nuestro servidor
spring:  
  application:
    name: configserver  #nombre de la aplicación
  cloud:
    config:
      server:
        git:
          uri: https://github.com/hjmendez/spring-cloud-config.git #url del git dónde están alojados los ficheros de configuración      